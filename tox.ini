[tox]
skipsdist=True
envlist =
    {py38}

[testenv]
deps = pipenv
commands =
    pipenv sync --dev
    pipenv run pytest tests -vv --cov=naval_warfare --cov-report=html --cov-report=term --cov-append
